const CustomerRegistrationLayout = ({
  children,
}: {
  children: React.ReactNode;
}) => {
  return <div className="flex justify-center">{children}</div>;
};

export default CustomerRegistrationLayout;
